import 'package:flutter/material.dart';

class PayMentView extends StatelessWidget {
  const PayMentView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(" SearchScreen Screen", style: TextStyle(fontSize: 22)),
    );
  }
}
